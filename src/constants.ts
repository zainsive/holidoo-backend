export enum ValidationSource {
  BODY = "body",
  PARAMS = "params",
  QUERY = "query",
}

export enum HttpMethods {
  GET = "get",
  POST = "post",
  PATCH = "patch",
  PUT = "put",
  DELETE = "delete",
}
